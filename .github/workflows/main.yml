- name: Find project folder
  run: |
    PROJECT_DIR=$(dirname "$(find . -maxdepth 4 -name settings.gradle -print -quit)")
    echo "PROJECT_DIR=$PROJECT_DIR" >> "$GITHUB_ENV"
    echo "Project found in: $PROJECT_DIR"
